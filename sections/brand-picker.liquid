{% comment %}
  Selector de marcas: tarjetas grandes, fijas, sin rotacion ni cambios de color al pasar el raton.
{% endcomment %}

<div class="color-{{ section.settings.color_scheme }} gradient">
  <div class="page-width section-{{ section.id }}-padding">
    {%- if section.settings.heading != blank -%}
      <div class="title-wrapper-with-link title-wrapper--no-top-margin">
        <h2 class="title amstershop-title {{ section.settings.heading_size }}">
          {{ section.settings.heading }}
        </h2>
      </div>
    {%- endif -%}

    {%- if section.blocks.size > 0 -%}
      <ul class="brand-picker-grid list-unstyled" role="list">
        {%- for block in section.blocks -%}
          <li class="brand-picker-grid__item" {{ block.shopify_attributes }}>
            <a href="{{ block.settings.link | default: '#' }}" class="brand-picker-grid__link">
              <span class="brand-picker-grid__media">
                {%- if block.settings.image != blank -%}
                  {{ block.settings.image | image_url: width: 1200 | image_tag: loading: 'lazy', sizes: '(min-width: 750px) 45vw, 90vw', widths: '400, 600, 900, 1200', class: 'brand-picker-grid__image' }}
                {%- else -%}
                  {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg brand-picker-grid__image' }}
                {%- endif -%}
              </span>
              <span class="brand-picker-grid__caption">
                {%- if block.settings.title != blank -%}
                  <span class="brand-picker-grid__title">{{ block.settings.title | escape }}</span>
                {%- endif -%}
              </span>
            </a>
          </li>
        {%- endfor -%}
      </ul>
    {%- endif -%}
  </div>
</div>

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }

  .brand-picker-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }

  @media screen and (max-width: 749px) {
    .brand-picker-grid {
      grid-template-columns: 1fr;
    }
  }

  .brand-picker-grid__item {
    margin: 0;
  }

  .brand-picker-grid__link {
    display: block;
    text-decoration: none;
    position: relative;
    border-radius: 16px;
    overflow: hidden;
  }

  .brand-picker-grid__media {
    display: block;
    aspect-ratio: 4 / 3;
    overflow: hidden;
  }

  .brand-picker-grid__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .brand-picker-grid__caption {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 18px;
    background: linear-gradient(to top, rgba(0,0,0,0.75), rgba(0,0,0,0));
  }

  .brand-picker-grid__title {
    color: #ffffff !important;
    font-size: 20px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .brand-picker-grid__link,
  .brand-picker-grid__link:hover,
  .brand-picker-grid__link:focus,
  .brand-picker-grid__image,
  .brand-picker-grid__link:hover .brand-picker-grid__image {
    transform: none !important;
    transition: none !important;
  }

  .brand-picker-grid__link:hover .brand-picker-grid__title,
  .brand-picker-grid__link:focus .brand-picker-grid__title {
    color: #ffffff !important;
  }
{%- endstyle -%}

{% schema %}
{
  "name": "Selector de marcas",
  "class": "section",
  "settings": [
    { "type": "text", "id": "heading", "label": "Titulo", "default": "Elige tu marca" },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        { "value": "h2", "label": "Pequeno" },
        { "value": "h1", "label": "Mediano" },
        { "value": "h0", "label": "Grande" }
      ],
      "default": "h1",
      "label": "Tamano del titulo"
    },
    { "type": "color_scheme", "id": "color_scheme", "label": "Esquema de color", "default": "scheme-1" },
    { "type": "range", "id": "padding_top", "min": 0, "max": 100, "step": 4, "unit": "px", "label": "Espacio superior", "default": 36 },
    { "type": "range", "id": "padding_bottom", "min": 0, "max": 100, "step": 4, "unit": "px", "label": "Espacio inferior", "default": 20 }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Marca",
      "settings": [
        { "type": "image_picker", "id": "image", "label": "Imagen" },
        { "type": "text", "id": "title", "label": "Titulo", "default": "Marca" },
        { "type": "url", "id": "link", "label": "Enlace" }
      ]
    }
  ],
  "presets": [
    { "name": "Selector de marcas" }
  ]
}
{% endschema %}
